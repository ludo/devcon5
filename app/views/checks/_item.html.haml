- content_tag_for(:tr, check, :class => cycle(:odd, :even)) do
  %td.status= h check.try(:status)
  %td.script= h check.script.name

  %td.actions
    = link_to 'Destroy', [@host,check], :confirm => 'Are you sure?', :method => :delete
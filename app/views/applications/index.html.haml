- content_for :subnav do
  = render :partial => "customers/subnav"

%h1.heading
  = "Applications"

.actions-bar
  %div{:class => "left search"}
    - form_for @search do |f|
      = f.label "Find:"
      = f.text_field :name
      = f.submit("Search")
  .right
    = will_paginate @collection, :prev_label => "&laquo; Back", :next_label => "Next &raquo;"
  .clear                                                                                     

%p.actions
  = link_to 'New Application', new_customer_application_path(@customer)
  
%table
  %thead.header
    %tr
      %th.name= 'Name'
      %th.actions= 'Actions'
  %tbody.items.applications
    - @applications.each do |application|
      = render 'item', :application => application

= will_paginate @collection, :prev_label => "&laquo; Back", :next_label => "Next &raquo;"
